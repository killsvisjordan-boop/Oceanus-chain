export const metadata = {
  title: 'Oceanus Chain',
  description: 'Oceanus Chain — $OCS Token & Arcade',
}

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}

